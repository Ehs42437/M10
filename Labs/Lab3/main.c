#include <stdio.h>
#include <stdlib.h>

//#define
//#ifdef ... #endif

/**EXAMPLE**/
/*
#define DEBUG
int main(){
    char x = 0;
    char y = 5;
    char res = 0;

    #ifdef DEBUG
        printf("%d\n",x);
    #endif

    res = y/x
    printf("%d", res);

    return 0;
}

initialize all of elements on array with zero
arr [10] = {0}; >> all going to be zero
to get size sizeof(arr)/sizeof(Type)

#include "string.h"

length of string strlen(array_name)
strncpy( destination, source, length);
strcmp(destination, source, length of smaller);
memcpy(destination, source, length of smaller);
memset(destination, replacedNum EX:0, length};
*/

int main()
{

    return 0;
}
