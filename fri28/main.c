#include <stdio.h>
#include <stdlib.h>

int main()
{
   int  x,c,y,z;
   for (z=0;z<=30;z++)
   {
      for(c=2;c<=30;c*=2)
      {
          c=0;
          c=x;

      }
      for( y=3;y<=30;y*=3)
      {
          y==0;
          y=x;

      }

   }
   printf("%d %d",x,z);
}
